{% block sw_order_detail_content_tabs_general %}
    {% parent %}
    <sw-tabs-item :route="{ name: 'sw.order.detail.unzerdirect', params: { id: $route.params.id } }"
                  :title="$tc('sw-order.detail.tabUnzerDirect')"
                  :disabled="isEditing">
        {{ $tc('sw-order.detail.tabUnzerDirect') }}
    </sw-tabs-item>
{% endblock %}
{% block sw_order_detail_actions_slot_smart_bar_actions %}
    <template v-if="!unzerdirectActive">
        {% parent %}
    </template>
{% endblock %}